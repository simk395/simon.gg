<img src="/summoners-rift.jpg" class="summoner-bg"/>

<div class="container">
    <div class="inner">
    <p class="summoner-name"><%= @summoner.summoner_name %></p>
    <img src="http://avatar.leagueoflegends.com/na/<%= @summoner.summoner_name %>.png" class="summoner-icon" alt="">
    </div>
</div>

<% num = 0 %>
<% person = 0 %>
<% @stats_arr = []%>
<% 10.times do |i| %>
  <%= form_for @game do |f| %>
    <%= f.hidden_field :game, value: "#{@matches[num]["gameId"]}" %>
    <%= f.submit "Check Game" %>
    <% players_hash = {} %>
    <% @matches[num]["participantIdentities"].each do |player|  %>
      <% if @summoner.summoner_name.downcase == player["player"]["summonerName"].downcase %>
        <% person = player["participantId"]-1%>
      <% end %>  <!--if-->
    <% end %><!--select-->
      <% if @matches[num]["teams"][0]["win"] == "Win" && person <= 4 %>
        <% players_hash["W/L"] = "Won" %>
      <% else %>
        <% players_hash["W/L"] = "Lost" %>
      <% end %><!--if-->
    <% stats = @matches[num]["participants"][person]["stats"] %>
    <% players_hash["kills"] = stats["kills"] %>
    <% players_hash["deaths"] = stats["deaths"] %>
    <% players_hash["assists"] = stats["assists"] %>
    <% players_hash["championID"] = @matches[num]["participants"][person]["championId"] %>
    <% players_hash["matchTime"] = @matches[num]["gameDuration"]/60 %>
    <% @stats_arr << players_hash %>
  <% end %><!--form_for-->
<% num = num + 1 %>
<% end %><!--times-->

<% def champs %>
  <% character=[] %>
  <% @stats_arr.each do |champions|%>
    <% @summoner.champions.each do |keys|%>
        <% if keys.include?(champions["championID"].to_s) %> 
          <% character << keys[1] %>
        <% end%>
      <% end%>
    <%end %>
    <% character %>
<% end%>

<div class="container">
    <div class="inner">
      <img class="champ-icon" src="/images/champion_icons/<%= champs[1] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[2] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[3] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[4] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[5] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[6] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[7] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[8] %>.png" alt="">
    </div>
</div>

<div class="container">
    <div class="inner">
    <img class="champ-icon" src="/images/champion_icons/<%= champs[9] %>.png" alt="">
    </div>
</div>


<!-- delete-->
<%= form_tag(summoner_path(@summoner), method: :delete) do %>
  <%= submit_tag "DELETE THIS account" %>
<% end %>


<!-- Display a card with basic profile information like rank and win/loss and top 3 champions -->

<!-- Display last 10 games with basic info on the games -->
